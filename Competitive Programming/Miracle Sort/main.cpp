#include <bits/stdc++.h>
typedef long long int ll;
using namespace std;
int n;
ll num;
int main ()
{
    scanf ("%d", &n);
    while (n--)
    {
        scanf ("%lld", &num);
        printf ("%lld\n", num);
    }
    return 0;
}
