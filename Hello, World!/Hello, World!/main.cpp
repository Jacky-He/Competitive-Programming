#include <bits/stdc++.h>
using namespace std;
int n;
int a, b;
string str1, str2;
int main ()
{
    cin >> a >> b;
    if (1 <= a && a <= 10 && 1 <= b && b <= 10)
    {
        cout << a + b << endl;
    }
    return 0;
}
